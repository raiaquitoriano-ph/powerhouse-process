# Web app URL (Deploy > New deployment > Web app)
VITE_SHEETS_API_URL="https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"

# If create fails for "Customer Acquisition" or other new steps: update your
# Apps Script (see apps-script/WebApp.example.gs) and/or add the step text to
# the Step column’s data validation in the Google Sheet.
